mutation UnloadOrderForAppraise($orderId: String!) {
  unloadOrderForAppraise(orderId: $orderId) {
    newFile{
      name,
      bucket,
      key,
      __typename
    },
    serverUrl,
    __typename
  }
}