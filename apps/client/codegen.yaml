overwrite: true
schema: './apps/client/schema.graphql'
documents: './apps/client/src/graphql/**/*.graphql'
generates:
  ./apps/client/src/types/graphql.ts:
    hooks:
      afterOneFileWrite:
        - eslint --fix
    plugins:
      - typescript
      - typescript-operations
      # - typescript-vue-apollo
    config:
      enumsAsTypes: true
      # withCompositionFunctions: true
      # vueCompositionApiImportFrom: vue
